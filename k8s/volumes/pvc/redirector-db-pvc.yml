---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: redirector-pv-volume
  labels:
    app: redirector
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi